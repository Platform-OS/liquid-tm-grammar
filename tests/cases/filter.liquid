{{ 42 | plus: tax.value | times: 100.0 | ceil }}

{{
  "foo.woff"
    | asset_url
    | upcase: as: 'font', media: 'mobile'
}}

{%
  echo
  "foo.woff"
    | asset_url
    | downcase:
      as: 'font',
      media: 'mobile'
%}

{%
  assign foo =
  "foo.woff"
    | asset_url
    | downcase:
      as: 'font',
      media:
        'mobile'
%}

{% liquid
  echo "foo" | plus: 1, arg: value
  assign x = "foo" | plus: 1, arg: value
%}
